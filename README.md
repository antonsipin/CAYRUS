
  # User dashboard with sections

  This is a code bundle for User dashboard with sections. The original project is available at https://www.figma.com/design/X6ag6evkORsko5ZlIwNuxZ/User-dashboard-with-sections.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  